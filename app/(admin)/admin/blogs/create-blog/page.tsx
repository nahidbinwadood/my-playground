import CreateBlogMainWrapper from './_components/create-blog-main-wrapper';

// Thin route shell. The wrapper owns the authoring layout.
const CreateBlogPage = () => {
  return <CreateBlogMainWrapper />;
};

export default CreateBlogPage;
