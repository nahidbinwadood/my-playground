import NotesMainWrapper from './_components/notes-main-wrapper';

// Thin route shell. The wrapper loads the blog list and the form owns the layout.
const NotesPage = () => {
  return <NotesMainWrapper />;
};

export default NotesPage;
